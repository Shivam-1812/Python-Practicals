# Create a tuple of numbers
numbers = (45, 23, 78, 12, 95, 34, 62)

# Find minimum and maximum
min_num = min(numbers)
max_num = max(numbers)

# Display results
print(f"Tuple: {numbers}")
print(f"Minimum number: {min_num}")
print(f"Maximum number: {max_num}")